{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKER",
    "buildCommand": "timeout 1800 docker build -t app ."
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 300
  }
}